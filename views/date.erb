<!DOCTYPE html>
<html>
  <head>
    <title>Date</title>
  </head>
  <body>
    <h1>Today</h1>
    <%= d = Datetime.new(%A %B %e %Y) %>
    <p>The date is <%= d.strftime('%A,%B %e, %Y') %></p>
  </body>
</html>